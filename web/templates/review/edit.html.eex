<h2>Edit review</h2>

<%= render "form.html", changeset: @changeset,
                        action: dropper_review_path(@conn, :update, @dropper_id, @review) %>

<%= link "Back", to: dropper_path(@conn, :show, @dropper_id) %>
